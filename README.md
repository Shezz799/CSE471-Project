# CSE471-Project

Skill-sharing platform (React + Express + MongoDB).

Copy `server/.env.example` → `server/.env` and `client/.env.example` → `client/.env.local`, then install and run:

```bash
cd server && npm install && npm run dev
```

```bash
cd client && npm install && npm run dev
```
